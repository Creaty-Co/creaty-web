name: "Release: pre release"

on:
  release:
    types: [ published ]

jobs:
  ci:
    uses: ./.github/workflows/call-ci.yml
    secrets: inherit
    with:
      tag: ${{ github.ref_name }}
      environ: 'stage'
